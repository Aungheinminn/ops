#!/usr/bin/env bun
import { parseCLISync } from '../src/cli/index.ts';
import { runTUI } from '../src/ui/index.tsx';

async function main() {
  const options = parseCLISync();
  await runTUI(options);
}

main().catch((err) => {
  console.error('Fatal error:', err);
  process.exit(1);
});
